import { FilterPrenotazioniPipe } from './filter-prenotazioni.pipe';

describe('FilterPrenotazioniPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPrenotazioniPipe();
    expect(pipe).toBeTruthy();
  });
});
